# Homebrew formula

This directory holds the Homebrew formula for Git Multi-Sync. The formula is used by the project’s tap so users can install with:

```bash
brew install sahilkamalny/homebrew-tap/git-msync
```

See the main [README](../../README.md) for installation and usage.
